{
  "name": "@aiverse/gateway",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run src/db/migrate.ts"
  },
  "dependencies": {
    "@aiverse/shared": "workspace:*",
    "@aiverse/topics": "workspace:*",
    "hono": "^4.6.0",
    "drizzle-orm": "^0.45.2",
    "postgres": "^3.4.0",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.28.0"
  }
}
