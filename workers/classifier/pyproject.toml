[project]
name = "classifier"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "jimkali", email = "kalinovjim@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "fastembed>=0.8.0",
    "psycopg[binary]>=3.3.4",
    "python-dotenv>=1.2.3",
    "vadersentiment>=3.3.2",
]

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
